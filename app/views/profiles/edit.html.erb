
<%= nested_form_for @profile do |f|%>
  <%= f.label :bio %><br />
  <%= f.text_field :bio %></div>

  <%= f.fields_for :bands do |bands|%>
    <%= bands.text_field :name %>
    <%= bands.link_to_remove "Band"%>
  <% end %>
  <%= f.link_to_add "Add Band", :bands %>

<%= f.submit %>
<% end %>